# tau-config agent notes

Before modifying `crates/tau-config`, read this directory's `ARCHITECTURE.md`, this directory's `design.md`, and the repository root `SECURITY.md`. Preserve the documented config layering, explicit error, alias normalization, role merge, extension-name, and atomic write contracts.
